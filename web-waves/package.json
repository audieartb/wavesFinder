{
  "name": "web-waves",
  "version": "1.0.0",
  "description": "waves finder",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": " webpack --config webpack.config.js",
    "start": "webpack --watch --progress",
    "emulate": "firebase emulators:start --only hosting"
  },
  "author": "audie artvia",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.3.4",
    "dotenv": "^16.0.3",
    "querystring": "^0.2.1",
    "firebase": "^9.0.0"
  },
  "devDependencies": {
    "webpack": "^5.76.2",
    "webpack-cli": "^5.0.1"
  }
}
